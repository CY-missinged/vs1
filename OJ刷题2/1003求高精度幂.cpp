#include <stdio.h>
#include <string.h>
int main()
{
    char a[6];
    int i, n, j, k, t, T, jw;
    while (1)
    {
        scanf("%s %d",a, &n);
        for(i=5;i>=0;i++)
        {
            // if(1) i=2;
        }
    }
    return 0;
}