// #include<iostream>
// using namespace std;

// #include<queue>  头文件
// queue<int> qq;   定义
        // （1）push()
        // push(x)可将x压入队列，时间复杂度为O(1)。实例见“queue容器内元素的访问”。
        // （2）front()、back()
        // front()和back()可分别获得队首元素和对微元素，时间复杂度为O(1)。实例见“queue容器内元素的访问”。
        // （3）pop()
        // 令队首元素出队，时间复杂度为O(1)
        // （4）empty()
        // empty()检测是否为空，返回true则为空，返回false则非空，时间复杂度O(1)。
        // （5）size()
        // size()返回queue内元素的个数，时间复杂度为O(1)。